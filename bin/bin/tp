#!/bin/sh

export DISPLAY=:0.0

tpctl() {
  for id in $(xinput | grep -e SYNA -e SynPS | grep -Po 'id=\K[0-9]*'); do
    xinput $1 $id
  done
}

case $1 in
  d*|D*)
    tpctl disable; echo Touchpad disabled!
    ;;
  *)
    tpctl enable; echo Touchpad enabled!
    ;;
esac
